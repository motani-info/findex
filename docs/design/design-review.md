# 設計レビューと是正（フラット第三者レビュー）

**作成日**: 2026-06-15
**位置づけ**: 設計フェーズ完結（D1〜D7・D5）後に、実装着手前へ向けて**フラットな第三者視点で全設計を点検**した記録と、各指摘への**是正方針・反映先**。本書は是正の索引であり、実体は各設計書の改訂とD8新設に落ちる。
**動機**: 設計品質が今後の製品品質を直接決める。「データだけ厳密でモデル・プロダクトが手薄」という重心の偏りを実装前に正す。

---

## 0. 総評

| 観点 | 評価 |
|---|---|
| データ基盤（柱1）の厳密さ | **高い**。自己認識・5状態Null・claim別グレード・N+・結果補正・来歴メタ・D2.5実証は多くの製品より堅い |
| 分析モデル（柱2）の妥当性 | **盲点**。重みは旧PJ継承の手づけ、スコアが何かを予測する実証が無い |
| プロダクト/読者（柱3） | **手薄**。読者像・競合・差別化テーゼ・出力物の実体設計が弱い |

→ 重心が「入力の正確性」に偏り、定款が警戒した完璧主義が**投資配分そのもの**に出ていた。本レビューでモデルとプロダクトの設計を引き上げる。

---

## 1. 指摘一覧と是正方針（重い順）

| # | 指摘 | 深刻度 | ユーザー判断 | 是正方針 | 反映先 |
|---|---|:--:|---|---|---|
| 1 | 入力は厳密だが**スコアモデルが無検証**（重みは旧PJ手づけ・予測力未実証） | ★★★ | **本格バックテスト基盤を構築** | **D8新設**。PIT（時点正確）入力で過去スコアを再現し、減配/増配/リターンとの関係を検証。重みはバックテストで較正（v5へ） | **D8**・D4・D4.5 |
| 2 | **読者/競合/差別化が未設計**（高配当株Xは飽和市場） | ★★★ | （#6と一体で判断） | D5に読者像・競合・1行差別化テーゼ（切り口①＝質で選ぶ増配株）を追加。テーマは絞って磨く | D5 |
| 3 | **YoC・バックテストの背骨がyfinance**（降格したはずの主ソース）で検証手段が無い | ★★★ | （フル設計維持） | D6に株価履歴の検証戦略（複数ソース突合・分割イベント独立検証・外れ値検知・信頼度上限）を追加 | D6・D3 |
| 4 | **EDINET XBRLパースがクリティカルパス最大リスク**（1社実証・基準別辞書は大工事） | ★★ | **全部設計通り（削らない）** | 削らず**リスクを設計で潰す**：早期スパイク・**パース成功率ゲート**・基準別辞書を検証可能な独立成果物化・段階ロールアウト | 実装計画・D6 |
| 5 | **単一総合スコア vs claim別グレードの比較可能性**（動的分母で銘柄間が同一物差しでない） | ★★ | （バックテスト判断に従属） | claim内ランキングを主、**total_scoreは最小採点指標数の足切り付き参考値**と明示。重み根拠はD8 | D4 |
| 6 | **出力チャネル（X自動投稿）が最も脆い**（ToS/レート/ログインブロック） | ★★★ | **Xメイン＋画像をローカルHTML生成（自前サイト兼用）** | HTML生成層を一級コンポーネント化（X画像＝サイト）。X自動化に手動承認フォールバック。助言コンプラ明文化 | D5・D7 |
| 7 | **配当深履歴が旧DB単一依存**（再現不能・かつ花王事故の当該データ） | ★★ | （フル設計維持） | 移行時に**yfinance配当1999+を再取得し移行データと相互照合**して旧DBの誤りを能動的に洗う | D7・D6 |
| 8 | ユニバース定義が曖昧（3,842 / 3,747 / 普通株） | ★ | — | charterで対象ユニバースを定義（普通株のみ・ETF/REIT/優先株除外・delistingで生存バイアス対策） | charter・D3 |
| 9 | post_log二重定義（D3とD5）・16/18反復・min_history曖昧・運用設計空白 | ★ | — | D3を単一正本に（themes/post_queue畳み込み）。運用（実行環境・実時間・通知）を実装計画に明記 | D3・実装計画 |

---

## 2. 是正の中身（要点）

### 2.1 モデル検証＝D8バックテスト基盤（#1・最重要）
- **PIT（Point-in-Time）原則**: as-of 時点で入手可能なデータだけでスコアを再現（先読みバイアス排除）。`result_overrides.as_of_fiscal_year` が既にPIT合成を支える設計になっている点を活用。
- **生存バイアス排除**: 上場廃止銘柄（`delisting_date`）を時点ユニバースに含める。
- **アウトカム**: 前方N年の減配有無・増配継続・トータルリターン。
- **評価**: スコア×アウトカムの順位相関・分位スプレッド・指標別IC・グレード較正（grade-A配当claimは本当に減配が少ないか）。
- **重み較正**: ウォークフォワード（訓練/検証分割）で過学習を防ぎ、解釈可能性を優先（ブラックボックス化しない＝透明性が製品価値）。
- 詳細は [D8 バックテスト基盤](08-backtest-framework.md)。

### 2.2 プロダクト/読者（#2・#6）
- **差別化テーゼ**: 「増配率の高さに騙されない、**質（EPS牽引）で選ぶ増配株**」＝切り口①を製品の中心に。汎用ランキングは誰でも出せる。
- **出力チャネル**: Xメイン。ただし投稿画像（ランキング表・チャート）は**ローカルHTMLで生成**＝自前サイトを兼ねる。サイトは確実に動きSEO資産にもなる。Xの脆い自動化は**手動承認フォールバック**付き。
- **コンプラ**: 個別銘柄の売買推奨はしない／免責定型／シミュレーションは前提明示。

### 2.3 データ品質の検証強化（#3・#7・#4）
- **株価履歴（yfinance 2000-2024）**: golden照合源が無い区間なので、複数ソース突合・分割イベント独立検証・外れ値検知でYoCの信頼度を担保（無理なら信頼度に上限を付け正直に）。
- **旧DB配当の能動洗浄**: yfinance配当1999+の再取得と移行データの相互照合。
- **EDINETリスク低減**: 削らず、パース成功率をゲート化し基準別辞書を独立検証（golden_financialsで基準を散らす＝D6既設計を前倒し運用）。

### 2.4 整合性（#5・#8・#9）
- ランキングはclaim内が主、総合は足切り付き参考値。
- ユニバース定義を確定（普通株のみ・約数）。
- D3を単一正本化（themes/post_queue/post_log）。運用設計を実装計画へ。

---

## 3. これによる設計フェーズの更新
- **新設**: D8 バックテスト基盤（モデル検証）。
- **改訂**: charter（ユニバース・モデル検証原則）/ D4（比較可能性・重みのバックテスト較正）/ D4.5（v4は暫定・v5はバックテスト較正）/ D5（読者・差別化・出力・コンプラ）/ D6（株価検証・旧DB照合・EDINETゲート）/ D7（バックテスト工程・移行照合・HTML生成）/ D3（themes/post_queue畳み込み・backtestテーブル・ユニバース）/ 実装計画（EDINETスパイク・バックテストPhase・運用）。
- 実装は引き続き凍結。本レビューは設計の質を上げる工程。

---

## 4. 実装での検証結果（Phase0-2・2026-06-15）

実装凍結解除後、取得層までで各指摘を実データで確認。詳細は [プロジェクトログ](project-log.html) 冒頭。

| # | 指摘 | 実装での結果 |
|---|---|---|
| 3 | yfinance株価の検証手段なし | ✅ **J-Quants×yfinance突合で最大乖離0.000%**。`close_adj`は分割のみ調整の`Close`（"Adj Close"=配当込みは不採用）。外れ値検知も実装 |
| 4 | EDINETパースが最大リスク | ✅ **基準別辞書を実証**（IFRS/JGAAP成立）。`AccountingStandardsDEI`で判別、連結ctx限定。**IFRSは投資有価証券/流動資産が構造的insufficient**、**US GAAP連結は構造化XBRL不在→grade_capitalフォールバック**。提出日日次スキャンで旧バグ是正 |
| 7 | 旧DB配当の能動洗浄 | 🟨 **「重複年照合」だけでは不足を発見**。haitoukinの分割調整状態が社により不統一→接合で単位統一＋混線はreviewフラグに強化。一部に単年seam穴（Phase3で橋渡し） |
| 8 | ユニバース定義・生存バイアス | ✅ 普通株=内国株式**3,734社**で確定（ETF/REIT/外国株/出資証券除外）。🟨 `delisting_date`の上場廃止銘柄backfillは未着手 |
| — | listing_date 取得（実装計画Phase1） | 🟨 **設計のkabutanはJS化で静的取得不可** → yfinance firstTradeDate主（床バンドNULL）＋kabutan(Playwright)補完を後続に |

**実装の教訓**: 設計で潰したつもりのリスクも、実データで叩くと前提のズレ（kabutan取得不可・床バンド・配当単位不整合・予想開示混入）が出る。各サブフェーズでコホート35社を検証してから進める方式が奏功（「実データが神」）。
